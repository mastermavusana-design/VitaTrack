# Flat node_modules (like npm/yarn) instead of pnpm's deep .pnpm nesting.
# Required for React Native / Expo on Windows: the default pnpm layout produces
# paths that exceed Windows' 260-char MAX_PATH during the native (CMake/prefab)
# build, and breaks Metro's resolution of expo-router/entry through symlinks.
node-linker=hoisted
